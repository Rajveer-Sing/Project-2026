name: Demo workflos
on:
  push:

jobs:
  job1:
    build:
      needs: [ dev ]
      name: Build
      steps:
        - name: Build and deploy on Cloud

  job2:
    dev: 
      name: Development
      steps:
        - name: Run the developer

  job3:
    test:
      needs: [ build, dev]
      name: Test
      steps: 
        - name: Testing the application
